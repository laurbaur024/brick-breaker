var http = require('http')
var fs = require('fs')

const PORT = 8080;

fs.readFile("./index.html", function(err, html){
        if(err){throw err};
        function onRequest(request,response){
                response.writeHead
        }
})